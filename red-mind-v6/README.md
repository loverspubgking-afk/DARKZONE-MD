# RED-MIND v6 — naya system (100% fresh code)

Purana code zero reuse. Nayi architecture:
- brain.py — naya ReAct loop (16 steps, ACT-DON'T-ASK rule, 1500-char results)
- tools.py — 22 naye tools (fresh implementations)
- omni.py — OmniRoute route (user ka gateway)
- ollama_local.py — Neural 14B route (Kaggle GPU, uncensored)
- memory.py — cross-task memory (naya feature)
- company.py — parallel workers (naya)
- server.py — naya FastAPI server

Run: `python3 server.py` (PORT env, default 7860)
